#include <iostream>
#include <vector>

class Solution {
  public:
    int minimumTotal(vector<vector<int> > &triangle) {
      if (triangle.length() == 1) {
        return triangle[0][0];
      }

    }
}
