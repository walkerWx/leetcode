#include<vector>

class Solution {
public:
  void sortColors(vector<int>& nums) {
    int begin = 0, end = nums.size() - 1;
    for (int i = 0; i != nums.size(); ++i) {
      if (nums.at(i) == 0) {
        
      }
    }
  }
}
